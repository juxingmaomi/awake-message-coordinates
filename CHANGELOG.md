# Changelog

## v1.3.0 - 2026-09-08 - Pre-release

- “我醒了”立即把本机睡醒日期与小时同步到当前预设的 `<薇薇睡醒时间>`，例如 `9月8日 13点`；下一条用户消息仍从 `#1` 开始。
- 只替换日期行，保留周围提示词、条目开关与顺序、未知字段及其他未保存的编辑；不重载或切换预设。
- “校正计数”增加一个原生补记窗口：选择醒来后的第一条用户消息，再填日期和 0 到 23 点。
- 补记创建独立的新周期，不搬走上一轮的起点。也可手动移动本轮正文标记，再用“修改本轮”补填时间。
- 时间与消息发送时刻分开保存，不根据消息日期猜测实际醒来时间。v1.2 消息锚点继续兼容。
- 同步失败保留待重试记录；重复点击不重置待发送的时间，重试不覆盖后来手动修改的日期。
- 生成中不允许修改清醒记录；弹窗打开后聊天、预设或消息发生变化时要求重新确认。
- 新起点与结束标记保存后回读对应聊天确认，防止酒馆保存接口吞掉异常时误报成功；普通消息不额外回读。
- 增加 40 项 Node 回归测试，以及桌面/手机尺寸的浏览器场景。浏览器使用独立脚本 iframe、已核对的原生 Popup 和 savePreset 实现，聊天及服务器仍为合成环境。
- 发布固定版本 CDN 入口与默认关闭的自包含入口；未远程部署，也未改动真实聊天或预设。

## v1.2.0 - Pre-release

- “我醒了”在下一条用户消息中保存独立的 `<awake_start>` 起点；不依赖发送时间或午夜。
- 用户和助手正文末尾写入 `<message_coordinates>`，保存总楼层和清醒后序号。
- 普通对话被记忆插件隐藏后仍计数；状态丢失时可从隐藏的起点消息恢复。
- 写入仅涉及正文及当前 Roll 的正文，不替换 reasoning、签名、Roll 元数据或消息 DOM。
- 总编号不再依赖清醒功能是否启用；未确认的清醒序号使用 `unknown`。
- 修复重 Roll 更新时间导致周期归属改变的问题；删除消息后重算，起点缺失时提示而不猜测。
- 保留可选“结束清醒”，改为在下一条用户消息保存 `<awake_end>`，不删除历史周期。
- 增加隐藏 40/80 条消息、状态恢复、保存失败重试、切换聊天及思维链保护测试。
- `npm run build:entry -- <输出目录>` 可生成默认关闭的自包含本地测试入口，无需发布 CDN 标签。

## v1.1.0 - 2026-08-15

- 新增“结束清醒”按钮，用于清零当前清醒周期。
- 清零后停止向模型上下文注入坐标，直到下一次点击“我醒了”。
- 旧入口会在加载新核心时自动补上按钮，只需修改 `VERSION`。

## v1.0.1 - 2026-08-15

- 删除生成提示中的楼层、重 Roll 与系统/工具消息规则说明，只保留本次对话坐标。

## v1.0.0 - 2026-08-15

- 首次 GitHub 版本化发布。
- 楼层尾标改为纯 DOM 显示，不再回写消息或 Roll 数据。
- 生成前临时注入真实楼层与清醒周期坐标，生成结束或停止后清空。
- 支持重 Roll、重新生成、继续生成、删除消息与旧变量迁移。
- 增加合成事件与 reasoning 完整性回归测试。
